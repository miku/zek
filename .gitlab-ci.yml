# Run test suite.
image: golang:1.25.0-trixie

cache:
  paths:
    - /go/src/github.com
    - /go/src/golang.org
    - /go/pkg

stages:
  - build

build:
  stage: build
  script:
      go test -v ./...
  tags: [docker]
